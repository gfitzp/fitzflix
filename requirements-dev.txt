# Development tools: formatting, linting, hooks, and tests.
# Includes the runtime requirements, so one install covers everything:
#   venv/bin/pip install -r requirements-dev.txt
-r requirements.txt
black==26.5.1
pre-commit==4.6.1
pyflakes==3.4.0
pytest==9.1.1
