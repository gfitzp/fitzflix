Hello,

The file '{{ file.basename }}' was added with a lossy first audio track but has a lossless track available.

Go here to compare the tracks and remux if necessary:

{{ url_for('main.lossy_audio_triage', file_id=file.id, _external=True) }}

- Fitzflix
