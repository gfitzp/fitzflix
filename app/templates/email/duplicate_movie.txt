Hello,

The movie '{{ movie.title }} ({{ movie.year }})' matched the TMDb record for '{{ canonical_title }} ({{ canonical_year }})', but it wasn't renamed because '{{ duplicate.title }} ({{ duplicate.year }})' already has that name. These two library entries appear to be the same movie.

Go here to review the new movie, and refresh its TMDb data to merge the two entries:

{{ url_for("main.movie", movie_id=movie.id, _external=True) }}

The existing movie is here:

{{ url_for("main.movie", movie_id=duplicate.id, _external=True) }}

- Fitzflix